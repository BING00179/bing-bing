pandas>=2.0
numpy>=1.24
yfinance>=0.2.40   # 미국장 시세. 국내장만 쓸 거면 없어도 됩니다.
pytest>=7.0        # 테스트 실행용
finance-datareader>=0.9.9  # 국내장 시세(한국거래소). 미국장만 쓸 거면 없어도 됩니다.
pykrx>=1.0.45          # 한국거래소 단면 데이터 (요인 검정용)
